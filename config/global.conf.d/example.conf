# Example drop-in configuration
# Place this file in /etc/global-sys-utils/global.conf.d/
#
# Drop-in files are loaded in alphabetical order after global.conf
# Use naming like: 10-myapp.conf, 20-another.conf for ordering

# Override settings for specific use case
# LOG_DIR = /var/log/myapp
# PATTERN = *.log
# PARALLEL_JOBS = 8
